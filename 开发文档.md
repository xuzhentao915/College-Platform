# 大学生综合服务开放平台系统 - 开发规范与指导文档

## 一、 开发环境要求
为保证团队协作和项目正常运行，开发人员需在本地配置如下基础环境：

| 依赖软件 / 工具 | 版本要求 | 备注 |
|-----------------|----------|------|
| **JDK** | Java 17 或 21 | Spring Boot 3 标准环境 |
| **Node.js** | v18.x 或以上 | 前端 Vite 构建环境 |
| **包管理工具** | Maven 3.8+ / pnpm 8+ | 建议配置对应的国内镜像源 |
| **数据库** | MySQL 8.0+ | 需支持 JSON 字段语法 |
| **缓存中间件** | Redis 7.0+ | 开启默认持久化即可 |
| **开发工具 IDE** | IntelliJ IDEA / VS Code | 建议安装 Lombok、Vue 插件 |

---

## 二、 代码结构设计规范

### 1. 后端工程结构 (Spring Boot 3 + MyBatis-Plus)
项目采用标准的分层 MVC 架构设计，目录划分如下：
```text
src/main/java/com/college/platform
 ├── config/      # 全局配置类 (Redis配置, Security配置, MyBatis配置等)
 ├── controller/  # RESTful 接口层 (按业务模块划分)
 ├── service/     # 业务逻辑层接口及实现子包 (impl)
 ├── mapper/      # MyBatis-Plus 数据访问层接口
 ├── model/       # 数据模型层 (entity: 数据库映射, dto: 请求体传参, vo: 视图展示)
 ├── security/    # 安全认证核心与 JWT 加解密工具
 ├── exception/   # 自定义业务异常 (BusinessException) 及全局处理器
 └── utils/       # 通用工具类 (加密解密, 格式转换等)
```

📌 **后端开发代码约定：**
- **统一响应**：API 统一封装返回格式，采用自定义泛型封装类（如 `Result<T>`）。
- **Lombok 使用**：实体类强制使用 `@Data` 或 `@Getter`/`@Setter`，必须使用 `@Builder`。利用 `@RequiredArgsConstructor` 实现依赖的构造器注入（替代 `@Autowired`）。
- **异常捕获机制**：禁止在控制层捕获异常并返回错误码。业务层统一抛出自定义异常，交由控制层的 `@RestControllerAdvice` 进行全局错误收敛及日志打印。
- **日志输出机制**：复杂逻辑利用 `@Slf4j` 记录运行状态，抛弃 `System.out.println` 以防堵塞。

### 2. 前端工程结构 (Vue 3 + TypeScript + Vite)
前端页面按功能就近划分目录：
```text
src/
 ├── api/         # 接口请求封装 (与后端的 controller 对应)
 ├── assets/      # 静态资源文件 (图片, SVG 图标, 全局样式表)
 ├── components/  # 全局可复用的公共组件 (如自定义图表、通用列表)
 ├── hooks/       # 提取的可复用 Composition API 逻辑 (如 useAuth)
 ├── layout/      # 页面基础布局框架 (顶部导航、侧栏菜单等)
 ├── router/      # Vue Router 路由配置及前端权限守卫
 ├── store/       # Pinia 状态管理 (如存储全局 UserInfo 等)
 ├── types/       # TypeScript 全局接口与类型声明
 ├── utils/       # 辅助工具函数 (Axios请求实例封装, 枚举常量)
 └── views/       # 业务视图核心页面
```

📌 **前端开发代码约定：**
- **类型安全**：TypeScript 配置为 `strict: true`，严格禁止滥用 `any` 魔术类型，网络请求拦截和返回结果需严格声明业务模型 `interface`。
- **异步请求**：一律采用 `async / await` 编写链路，结合原生 `try-catch` 或全局响应拦截来进行异常通知提示，杜绝 Callback Hell。
- **组件规范**：采用 `<script setup lang="ts">` 单文件组件模式编写代码。为了维护性，单文件长度尽量控制在 300 行内。

---

## 三、 API 接口通信与鉴权规范

### 3.1 RESTful 响应结构标准
后端所有对外暴露的 API，强制统一返回标准的 JSON 结构：
```json
{
  "code": 200,                // 业务状态码 (200=成功, 401=授权失效, 500=系统兜底异常)
  "message": "操作成功",        // 面向用户的中文提示信息
  "data": { ... }             // 实际荷载数据，如为分页查询则必须包含 records 和 total
}
```

### 3.2 身份认证鉴权 (JWT)
前端登录系统校验成功后获得 `token`。此后对于所有的受保护接口请求头部，必需携带授权凭证：
```http
Authorization: Bearer <Your_JWT_Token>
```
若 Header 中未包含 Token 或 Token 解析失败，后端将统一拦截并上抛 `401 Unauthorized` 状态代码。

---

## 四、 数据库与 SQL 开发规范

1. **规约定义**：主表主键统一使用自增数字 `bigint`，布尔字段统一以前缀 `is_`、`has_` 命名。
2. **安全审计机制**：所有核心业务表必须包含 `create_time` 和 `update_time` 字段，并利用 MyBatis-Plus 自动填充插件管理其生命周期。
3. **软删除策略**：涉及用户的交易订单订单、成绩表、借阅记录等敏感记录不允许进行物理删除。设置 `is_deleted` 布尔约束字段用作逻辑删除标识。
4. **防 SQL 注入**：严格利用 MyBatis 的参数化查询(`#{}`)或利用 MP Wrapper 动态拼接查询，**严禁拼接原生 SQL 字符串**。

---

## 五、 Git 分支管理与协作约束

请严格按照 Git Flow 的轻量演进版本管理生命周期：

- **主分支 `main`**：随时保持可发布的稳定无缺状态。
- **特性分支 `feature/xxx`**：每开发一个新模块时（如 `feature/flea-market` 从主分支拉取，开发自测通过后发起 Merge Request）。
- **修复分支 `fix/xxx`**：用于修复环境中的线上缺陷。

**提交信息规范约束（Commit Message）：**
团队成员提交代码需确保本地可以成功编译运行，并参照标准前缀提交：
- `feat: [交易模块] 增加图片上传功能`
- `fix: [借阅模块] 修复归还日期计算跨年错误`
- `refactor: [核心] 重构 JWT 验证抽取为过滤器`
- `docs: 更新 README 及部署文档`
