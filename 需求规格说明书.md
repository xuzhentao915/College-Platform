# 大学生综合服务开放平台 - 软件需求规格说明书 (SRS)

## 一、 引言
### 1.1 编写目的
本文档详细梳理并固化了“大学生综合服务开放平台”系统的各项功能及非功能需求，旨在为后续的系统架构设计、数据库搭建、前后端代码编写及系统测试提供唯一标准指导。

### 1.2 系统范围
本系统是一个面向高校在校师生的综合型生活与学习服务平台。系统整合了校园内最高频使用的几大版块，包括：校园二手跳蚤市场、图书馆借阅辅助管理、教务成绩统计分析以及学术文献分享互动，并以统一的门户首页进行数据聚合展现。

---

## 二、 用户角色与用例分析
系统通过 RBAC 访问控制模型设计以下核心使用角色：
1. **学生 (Student)**：平台的主要活跃使用者。可以进行个人成绩查询、二手物品发布与购买、文献检索收藏、图书续借。
2. **教师/辅导员 (Teacher)**：拥有部分管理权限的用户。除基本功能外，可发布学术前沿文章，浏览所负责班级的脱敏成绩概况（预留扩展）。
3. **图书管理员 (Librarian)**：专属模块管理员，负责馆藏书籍录入、更新报废库存及处理违规借阅。
4. **超级管理员 (Admin)**：管控全局。包括系统基础字典表维护、违规二手商品强制下架、全局防骗通知发布及各项运维数据看板。

---

## 三、 核心功能需求说明

### 3.1 统一安全与用户中心模块
- **SSO 与登录**：支持学号/工号 + 密码的安全登录（后端密文比对，不传输明文密码）。
- **安全验证机制**：鉴权阶段集成图片验证码防暴刷。用户中心支持邮箱动态验证码重置/找回密码。
- **个人档案维护**：用户可修改上传头像，完善寝室联系地址、个性签名等。

### 3.2 门户首页信息聚合模块 (Portal)
- **全局搜索引擎**：顶部的万能搜索框，可通过下拉前缀选择检索类别（如：查书、查闲置、查论文）。
- **动态 Banner 轮播**：由管理员后台配置运营位图片，用于展示校园大事件等。
- **金刚区快捷入口**：提供醒目、易点击的各子业务系统的可视化图标跳转导航。
- **个性化信息流（猜你喜欢）**：瀑布流展示最新上架的低价闲置、临近还书日的动态提醒。

### 3.3 二手闲置交易模块 (Flea Market)
- **商品发布与打标**：学生可上传多张商品图片，设定主副标题、价格区间，并挂载品类（数码、教辅、生活、美妆）。
- **商品状态机流转**：商品生命周期包含：`待审核 -> 售卖中 -> 交易锁定 -> 已售出 / 已冻结下架`。
- **互动寻价机制**：采用轻量化“讨论板/留言板”机制，买卖双方可在线公开询价与回复（避免重度 IM 开发）。
- **我的橱窗库**：管理个人的闲置清单，支持一键催曝光、价格修改与上下架管理。

### 3.4 图书管理与借阅模块 (Library)
- **馆藏元数据检索**：支持按照 ISBN 编码、书名分词、第一作者、出版社名称进行组合高级检索。
- **借阅状态可视**：列表页即刻显示图书剩余可用馆藏量及排架号。
- **个人借阅时间轴**：通过流式布局展现当前用户的在借书目起止时间、历史逾期记录。
- **超期闭环管理**：临近超期自动 UI 标红警示，并支持一键强续借（限定每本最多续借 1 次）。

### 3.5 教务与成绩管理模块 (Academic)
- **成绩清单速查**：分页+折叠板结构化展示各学期的所修课程类型（必修/选修）及考试实际得分。
- **GPA 与学情图表分析**：系统对已出成绩自动进行学分与绩点的高速对冲加权，通过 ECharts 提供成绩优劣势雷达图。
- **挂科与重修预警**：对低于 60 分（或考核不合格）且未获得关联学分的课程，在成绩板列表置顶置红提示。

### 3.6 学术期刊共享模块 (Journal)
- **文献库浏览区**：聚合呈现接入的重量级论文条目及引言数据。
- **学术收藏夹系统**：用户可将多篇文献统一加入“我的课题文献夹”，支持自行创建目录夹标记分类。
- **学术交流互动区**：针对单篇高质量文章的主题探讨评论树（支持父子层级盖楼）。

---

## 四、 非功能性需求约束

### 4.1 性能与系统承载力
- **页面响应度**：前端系统非首次资源加载页面的路由切换需满足在 `1.0秒` 内完成呈现，接口 API 请求不长期 pending。
- **高频并发优化**：高频公共接口（如首页各模块聚合入口、最新二手商品列表）强制挂载通过 Redis 缓存，保证高峰期抢课刷接口时平台不产生级联雪崩。

### 4.2 安全与稳定性监控
- **越权深度防护**：前端按钮隐藏与后端接口强隔离并发执行。严格防范水平越权问题（例：利用 API 篡改删除其他用户的二手商品），后台 `Controller` 取用户的 UID 需强依赖自定解析 Token 获取，而非前端传参。
- **防 XSS 及 SQL 注入攻击**：涉及所有前端的 HTML 输入采用文本安全转义；持久化查询采用严格定义的参数化。

### 4.3 UI/UX 设计及工程美学约束
- **色彩设计美学**：彻底抛弃早期陈旧的管理后台界面风格。强依赖现代主义设计：运用圆角卡片、低饱和度莫兰迪色或大厂 UI 主题蓝、骨架屏加载过渡等。
- **无感化交互反馈**：网络请求等待期自动禁用提交按钮并显示 Loading 指示，请求结果必须使用全局不阻断画面的 `Toast` （轻提示）组件优雅呈现结果。
